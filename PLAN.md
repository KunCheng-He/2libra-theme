# 2libra-theme 开发方案

> 为 2libra.com 打造浏览器插件级「主题伪装」系统。第一版主题：企业微信（桌面端风格）。
> 状态：方案已确认（v1 范围见 §7 决策记录），未开始开发。

---

## 1. 项目概述

### 1.1 目标

开发一个 Chrome 扩展（Manifest V3），将 **2libra.com**（工作与生活平衡社区）的界面整体重塑为**企业微信桌面客户端**的外观与交互，使浏览 2libra 的体验看起来像在使用办公 IM，实现深度伪装。

后期以「主题包」机制扩展更多伪装主题（如 IDE / 终端 / 飞书 / Excel 等），本期只交付主题引擎 + 企业微信主题。

### 1.2 产品形态

| 项 | 决策 |
|---|---|
| 载体 | Chrome 扩展，Manifest V3 |
| 伪装深度 | **深度伪装**：整页接管，重塑为企业微信三栏布局，帖子渲染为会话/消息流 |
| 多主题 | 引擎 + 主题包分层架构，主题包可插拔 |
| 发布 | v1 内部使用（开发者模式加载，zip/crx 分发）；上架 Chrome Web Store 为后续目标 |

### 1.3 非 目标（v1 不做）

- 老板键 / 原版⇄伪装瞬时切换（伪装为常驻状态，不需要）
- 第二主题的具体实现（仅保证 ThemePack 接口抽象成立）
- Firefox / Edge / Safari 适配
- 移动端布局
- 2libra 站外页面的伪装

---

## 2. 目标网站分析（2libra.com）

通过抓取首页 HTML 确认的关键事实：

- **技术栈**：Next.js（App Router，客户端水合 SPA）+ daisyUI / Tailwind，PWA（有 manifest.json、动态 favicon）
- **路由结构**：
  - `/` 帖子流首页
  - `/post/{节点slug}/{帖子ID}` 帖子详情（如 `/post/social-observation/q83qgmh`）
  - `/node/{分类}/{子分类}` 节点页（如 `/node/daily-life/personal-life`）
  - `/user/{name}/about` 用户页
  - 顶部节点 Tab：产品 / AI / 技术 / 创作分享 / 效率工具 / 管理协作 / 职场发展 / 创业副业 / 生活日常 / 阅读观影 / 游戏娱乐 / 问与答 / 旅行户外 / 科技数码 / 社交活动 / 情感生活
- **主题机制**：站点自带 daisyUI CSS 变量体系（`--color-base-100`、`--color-primary` 等），`localStorage.custom_daisyui_themes` 支持自定义配色 —— 但**只改颜色不改布局**，无法满足深度伪装需求，本插件需自行接管渲染。
- **SPA 特性**：Next.js Link 软导航，DOM 由 React 客户端渲染，类名经过处理（不可依赖具体 class 做稳定选择器）。

**对架构的直接影响**：
1. React 会反复重写 DOM → 与其对抗不如绕开（见 §4.2 整页接管策略）。
2. 数据获取需要逆向站点内部 API 或拦截请求（见 §4.3 数据层，M0 优先验证）。
3. SPA 路由变化需监听（`history.pushState` 劫持 + `popstate`）。

---

## 3. 产品设计（企业微信主题）

### 3.1 布局映射

企业微信桌面端为经典三栏结构，与 2libra 内容的映射关系：

```
┌──────┬────────────────┬──────────────────────────────┐
│ 导航栏 │    会话列表      │         聊天窗口               │
│ (窄深) │                │                              │
│       │                │  ┌─ 顶栏: 帖子标题 / 节点名 ─┐   │
│ 头像   │ 搜索框(真实搜索)  │  │  消息流:                  │   │
│ 消息 ● │ 帖子1 ▸ 标题     │  │   [作者头像] 楼主正文      │   │
│ 文档   │      摘要  时间   │  │   [作者头像] 回复1        │   │
│ 日程   │ 帖子2           │  │   ...                    │   │
│ 会议   │ ...            │  └─ 输入框: 回复本帖 ────────┘   │
│ 工作台 │                │                              │
└──────┴────────────────┴──────────────────────────────┘
```

| 企业微信元素 | 2libra 对应内容 | 交互 |
|---|---|---|
| 左侧导航（消息/文档/日程/会议/工作台） | 「消息」= 帖子流；其余为装饰性入口，点击弹 toast「已在工作台打开」类假反馈 | 假交互 |
| 会话列表 | 帖子列表：头像=节点图标/作者头像，标题=会话名，摘要=正文预览，时间=发布/最后回复时间，未读红点=新回复数 | 点击 → 聊天窗口加载帖子详情（SPA 内切换，同步 URL） |
| 节点分类 | 映射为顶部标签页条（企业微信 4.x 多会话标签样式）或左侧分组 | 点击切换 = 访问对应节点 |
| 聊天窗口 | 帖子详情：楼主=首条消息，**楼层回复=「引用回复」气泡**（引用原楼层内容 + 回复正文） | 正常浏览与回复 |
| 输入框 | 回复框 | **真实回复**（调用站点提交接口） |
| 头像（会话/消息流内） | 用户头像 | 点击暂不跳转（v1） |
| **搜索框**（会话列表顶部） | 站内搜索 | 点击展开搜索面板，**真实调用搜索接口**，结果（帖子/节点/用户）可直接跳转 |
| **「发起群聊 / 新建群聊」** | 发帖 | 伪装为新建群聊页：**选节点≈选联系人**、标题+正文表单，提交即**真实发帖** |
| **左侧导航头像（我的）** | 个人中心 | 点击弹出企微风格**身份卡片**：头像/昵称/ID/我的帖子等入口 |

### 3.2 核心功能清单（v1）

1. **常驻伪装**：进入 2libra 任意页面自动启用主题并始终保持伪装态；不做老板键，启停仅通过 popup 控制
2. **真实浏览与回复**：聊天窗口底部输入框可**真实发表回复**（调用站点提交接口，失败显式报错）；楼层回复在消息流中以「引用回复」气泡展示（引用原楼层内容 + 回复正文）
3. **真实发帖**：伪装为「新建群聊」页面——选节点（≈选联系人/部门）、标题 + 正文，提交即真实发帖
4. **真实搜索**：会话列表顶部搜索框展开搜索面板，真实调用站内搜索，结果可直接跳转
5. **个人中心**：点击左侧导航头像弹出企微风格**身份卡片**（头像、昵称、ID、我的帖子等入口）
6. **SPA 导航**：伪装 UI 内点击/前进/后退正常工作，URL 与真实路由保持同步（刷新后仍在伪装态）
7. **回退保护**：访问登录页、设置页等剩余未映射路由，或站点改版探测失败时，自动恢复原版界面——**保证站点原始功能始终正常**
8. **Popup 面板**：启用/停用、主题选择（v1 仅企业微信，其余主题灰置占位）
9. **状态持久化**：启用状态、当前主题存 `chrome.storage.local` / `sync`

### 3.3 视觉规格（需 M0 阶段截图采样校准）

- 左侧导航栏：深灰黑（约 `#1E1E1E ~ #2A2A2A`），图标线性风格，选中高亮企业微信蓝（约 `#0082EF`）
- 会话列表：浅灰底（约 `#F5F6F7`），选中项高亮
- 聊天区：浅灰白底，他人消息白底气泡+头像，自己消息绿底气泡（约 `#95EC69`，以实测为准）
- 字体：系统默认 -apple-system / PingFang SC / Microsoft YaHei 栈
- 细节还原清单：窗口顶部假标题栏、未读红点、时间分割线（“上午 10:23”）、**引用回复气泡（楼层回复）**、@我样式、图片/链接消息卡片、底部输入框工具条（表情/截图/发送按钮，仅装饰）

---

## 4. 技术架构

### 4.1 分层总览

```
┌─────────────────────────────────────────────┐
│  Chrome Extension (MV3)                     │
│                                             │
│  popup/        启停控制、主题切换 UI           │
│  background/   service worker：badge、        │
│                快捷键、状态中转（无状态，         │
│                一切持久化走 storage）          │
│  content/      注入 2libra.com：             │
│  ┌─────────────────────────────────────┐    │
│  │ Engine（主题引擎，与主题无关）           │    │
│  │  · 站点识别 / 版本探测                 │    │
│  │  · 路由劫持与同步                      │    │
│  │  · 主题包加载、mount/unmount 生命周期   │    │
│  │  · 异常自动回退保护                    │    │
│  ├─────────────────────────────────────┤    │
│  │ Data Adapter（站点数据适配层）          │    │
│  │  · 统一数据模型: Post / Reply / User   │    │
│  │  · API Client（same-origin 带凭据）    │    │
│  ├─────────────────────────────────────┤    │
│  │ Theme Registry（主题注册表）            │    │
│  │  └─ themes/wecom  企业微信主题包        │    │
│  │      · Renderer + 模板 + CSS + 素材    │    │
│  └─────────────────────────────────────┘    │
│  shared/       类型定义、消息协议、storage key  │
└─────────────────────────────────────────────┘
```

核心原则：**引擎不认识具体主题，主题不关心站点细节**。后续新增主题 = 只写一个新的 ThemePack。

### 4.2 整页接管策略（关键决策）

2libra 是 React SPA，直接改其 DOM 会与 React 重渲染冲突。方案对比：

| 方案 | 做法 | 问题 |
|---|---|---|
| A. CSS 换肤 + DOM 包装 | 在真实 DOM 上做样式映射 | React 重渲染/类名失效，伪装深度不够 |
| B. **整页接管（选定）** | 隐藏原 App 根节点（保留运行），插件在独立 Shadow DOM 根中渲染自己的企业微信 UI；数据通过 same-origin 请求站点接口获取 | 需逆向数据接口；需自实现交互（回复、发帖、搜索等） |
| C. iframe 嵌套 | 隔离渲染 | 多余复杂度、登录态/滚动/弹层问题 |

选定 **方案 B**：

- 原 React 应用整体 `display:none`（不卸载，保持会话与全局状态存活），接管/恢复都只是切换显隐，秒级回退。
- 主题 UI 挂载在自建根节点 + **Shadow DOM**，样式完全隔离，防站点 CSS 污染与反方向泄漏。
- 伪装 UI 内部用轻量自实现路由（`history.pushState` + `popstate`），URL 始终是真实 2libra 路由，深链接、刷新、分享均不受影响。

### 4.3 数据层（M0 重点技术验证）

主题 UI 需要结构化数据（帖子列表/详情/回复/用户）。候选策略按优先级：

1. **策略 A：直接调用站点内部接口**（首选；**因回复/发帖必须真实可用，此策略是硬需求**）
   - 逆向 2libra 的数据接口：**读**（帖子列表 / 详情 / **搜索**，REST（`/api/*`）、Server Actions、或 RSC payload（带 `RSC: 1` 头 fetch 返回可解析 JSON））+ **写**（**发帖**、发表回复的提交链路）
   - same-origin `fetch(..., { credentials: 'include' })` 自动携带登录 Cookie，登录态天然可用
2. **策略 B：MAIN world 请求拦截**（仅读数据兜底）
   - 注入 `world: "MAIN"` 内容脚本，Hook `window.fetch` / XHR，把站点自身发出的数据响应镜像到隔离世界，被动获取数据
   - 站点改版时鲁棒性更好，但只能“跟着站点走”、无法代发写请求，因此只作为列表/详情数据的降级来源
3. **策略 C：从隐藏的原 App DOM 提取**（兜底）
   - 语义化结构（`<a href="/post/...">`、article 列表）做解析，不依赖哈希类名
   - 开发成本高、易碎，仅在 A/B 均不可行时使用

> M0 里程碑产出：确定主策略 + 兜底策略，并给出「帖子列表 / 详情 / 搜索 / **发帖 + 回复（写）**」数据通路的最小可用 Demo（写链路在测试账号下验证）。

### 4.4 主题包接口（多主题扩展点）

```ts
// shared/types.ts
interface ThemePack {
  id: string;                 // 'wecom'
  name: string;               // '办公 IM 风格'
  version: string;
  routes: RouteMap;           // 声明支持的路由 → 渲染器映射，未覆盖路由触发回退
  mount(ctx: ThemeContext): Promise<void>;   // 渲染 UI（ctx 含 dataApi、router、storage）
  unmount(): Promise<void>;
}

interface ThemeContext {
  root: ShadowRoot;           // 引擎提供的挂载根
  data: DataApi;              // §4.3 数据适配层的统一接口
  router: RouterApi;          // push/replace/subscribe，URL 同步
  escapeHatch(): void;        // 请求回退原版（异常时主题自行调用）
}
```

- 引擎在 `manifest` 中按需声明各主题 chunk，`import()` 动态加载当前主题，未启用主题的代码不进入内存。
- 新主题（如 `ide-dark`）只需新增 `themes/ide-dark/` 实现同一接口，注册进 Registry。

### 4.5 目录结构（预设）

```
2libra-theme/
├── PLAN.md
├── CHROMEWEBSTORE.md        # 上架前建立：商店上架信息唯一事实源（v1 内部使用不需要）
├── manifest.json
├── src/
│   ├── background/          # service worker（可选：badge 等增强，v1 可省略——popup 直写 storage，content 监听 storage.onChanged）
│   ├── content/
│   │   ├── engine/          # 主题引擎：探测/路由/生命周期/回退保护
│   │   ├── data/            # 数据适配层：模型 + API client
│   │   └── themes/
│   │       ├── registry.ts
│   │       └── wecom/       # 企业微信主题包
│   │           ├── index.ts
│   │           ├── components/   # 侧栏/会话列表/聊天窗/输入框
│   │           ├── wecom.css
│   │           └── assets/       # 图标、假头像等
│   ├── popup/
│   └── shared/              # 类型、消息协议、常量
├── scripts/                 # 图标生成、打包脚本
└── e2e/                     # Playwright 冒烟测试（M4，可选）
```

工具链：TypeScript + Vite + CRXJS（备选 WXT 框架）。MV3 红线：无内联脚本、全 async/await、SW 不存内存状态、`tabs` 权限按需申请。

---

## 5. 里程碑计划

| 里程碑 | 内容 | 产出 / 验收标准 |
|---|---|---|
| **M0 技术验证**（先行） | ① 逆向 2libra 数据接口（读 + **发帖/回复写链路** + 搜索），确定 §4.3 主/兜底策略；② 企业微信桌面端截图采样，产出设计 token（配色/字号/间距/图标）；③ 验证隐藏原 App + Shadow DOM 接管在 2libra 上可行 | 数据通路 Demo（含发帖/回复写链路）；`wecom-tokens.css` |
| **M1 引擎骨架** | MV3 脚手架、站点探测、整页接管与恢复、路由劫持同步、popup 启停、回退保护 | 伪装态常驻稳定，刷新/前进后退不丢状态，原始站点功能不受影响 |
| **M2 数据适配层** | Post/Reply/User 模型、API client（读 + 发帖/回复提交 + 搜索）、列表分页、错误与改版探测 | 引擎可向主题稳定供给全部页面数据，发帖/回复可真实提交 |
| **M3 企业微信主题渲染** | 三栏布局、会话列表、聊天消息流（含楼层回复的「引用回复」气泡）、真实回复输入框、**新建群聊式发帖页、搜索面板、身份卡片**、SPA 内导航、装饰性假交互 | 全站可“以假乱真”地浏览、回复、发帖、搜索 |
| **M4 打磨发布（内部）** | 视觉细节校准、性能（长列表虚拟滚动）、图标与打包（开发者模式 zip/crx，内部分发） | v1.0 内部日常可用 |

> 后续展望（不在 v1 范围）：接入第二主题验证 ThemePack 抽象；上架 Chrome Web Store（届时补 CHROMEWEBSTORE.md、商标中性命名、隐私政策与截图素材）。

---

## 6. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 站点接口不可逆向 / 改版频繁 | 数据层失效 | A/B/C 三级策略降级；适配层做版本探测，失败自动回退原版并提示 |
| React 与接管层冲突（热更新、弹层、登录跳转） | 界面异常 | 原 App 仅隐藏不卸载；全局错误边界 → `escapeHatch()` 自动回退 |
| 长列表性能 | 会话/消息流卡顿 | 虚拟滚动；批量 DOM 更新（`requestAnimationFrame`） |
| 登录态/CSRF | 回复失败 | same-origin 带凭据请求；回复失败显式报错，不做静默假成功 |
| **商标与商店审核**（上架阶段才成为现实风险） | “伪装企业微信”命名涉嫌商标侵权；CWS 对伪装类工具审核敏感 | 内部使用阶段风险可控；上架前完成命名中性化（“办公 IM 风格主题”）、商店描述定位为“为 2libra 提供界面主题”（不出现“企业微信/WeCom/腾讯”字样）、图标原创 |
| 隐藏原 App 仍在后台运行 | 内存占用 | 可接受（保活换稳定）；M4 实测优化 |

---

## 7. 决策记录（已确认）

| # | 决策 |
|---|---|
| 1 | **回复功能真实可用**：帖子详情整体渲染为消息流；楼层回复以「引用回复」气泡形式展示（引用原楼层内容 + 回复正文），输入框为真实提交 |
| 2 | **不做老板键**：插件开启后伪装状态常驻；站点原始功能始终正常（数据真实、未覆盖页面自动回退原版 UI） |
| 3 | **v1 内部使用**（开发者模式加载 / zip-crx 分发），上架商店为后续目标，届时再补 CHROMEWEBSTORE.md、商标中性命名与隐私政策 |
| 4 | **第二主题不在 v1 范围**，仅保证 ThemePack 接口抽象成立 |
| 5 | **发帖 / 搜索 / 个人中心纳入 v1 完整伪装**：发帖 =「新建群聊」页面（选节点≈选联系人）；搜索 = 集成到会话列表顶部搜索框（真实调用站内搜索）；个人中心 = 企微风格身份卡片 |
